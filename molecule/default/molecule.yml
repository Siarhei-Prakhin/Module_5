---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: localhost:5000/mycentos8
    pre_build_image: true
#    tmpfs:
#      - /run
#      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
provisioner:
  name: ansible
verifier:
  name: testinfra
